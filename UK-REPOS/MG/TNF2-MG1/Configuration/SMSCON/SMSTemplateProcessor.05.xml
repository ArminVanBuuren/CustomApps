<templates connector="SMS">
	<template procedureName="DeliveryRequestTest" persistent="false" reportDelivery="true">
		<condition>
			<test item="0" matches="1" />
		</condition>
		<deliveryTemplate>
			<set item="Content" value="${no-escape:Content}" />
			<set item="Sender" value="${Sender}" />
			<set item="Recipient" value="${customer}" />
			<set item="Encoding" value="${Encoding}" />
			<!--set item="ValidityPeriod" value="000000000500000R" /-->
			<set item="RegisteredDelivery" value="${RegisteredDelivery}" />
		</deliveryTemplate>
	</template>
	<template procedureName="SMSIncomingContent" persistent="false">
		<condition>
		</condition>
		<receiveTemplate>
			<Message type="incomingcontent" initiator="smscon" id="${newGuid}" session_id="@{newGuid}">
				<Channel id="smscon"/>
				<Item recipient="${Recipient}" sender="${Sender}" timestamp="@{now}">
					<Content charset="${encoding}">${Content}</Content>
				</Item>
			</Message>
		</receiveTemplate>
		<deliveryTemplate direction="input">
			<set item="Content" value="Success"/>
                        <!--set item="Content" value="${no-escape:Content}"/-->
			<set item="Recipient" value="${Sender}"/>
			<set item="Sender" value="${Recipient}"/>
                        <set item="encoding" value="iso-8859-1"/>
			<!--set item="ValidityPeriod" value="000000020000000R" /-->
		</deliveryTemplate>
	</template>	
</templates>
